const baseURL = import.meta.env.VITE_API_URL;

const projectName = import.meta.env.VITE_APP_NAME;

const hashSecret = import.meta.env.VITE_APP_HASH_SECRETE;

export { baseURL, hashSecret, projectName };
